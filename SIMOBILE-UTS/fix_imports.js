const fs = require("fs"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + "/" + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { results.push(file); } }); return results; } const files = walk("src/app").filter(f => f.endsWith(".module.ts")); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); if (content.includes("from '@ionic/angular'")) { content = content.replace(/from '@ionic\/angular'/g, "from '@ionic/angular/lazy'"); fs.writeFileSync(f, content); console.log("Fixed " + f); } });
